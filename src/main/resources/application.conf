ktor {
  development = true
  deployment {
    port = 8080
    port = ${?PORT}
    watch = [recource, classes]
  }
  application {
    modules = [com.cqsd.ApplicationKt.module]
  }
}
jwt {
  # 发行域
  domain = "https://jwt-provider-domain/"
  # 中间人
  audience = "jwt-audience"
  # 发行人
  issuer = "http://0.0.0.0:8080"
  realm = "ktor sample app"
  secret = "kfhasdfgsiaihesgjairgsdkfhjuergyfgdsjhfaueygafwudjhhguaksgfw"
}
# TODO 使这里可以配置多数据源
dataSources = [
  {
    # 标识数据源的名字
    name = "test"
    drivelClassName = "com.mysql.cj.jdbc.Driver"
    url = "jdbc:mysql://49.232.150.194:3306/test"
    username = "root"
    password = "5201314zFy@"
    # initialSize = 5
    # maxActive = 50
    # minIdle = 5
    # 指示是否不用等待自己连接完毕立即启动
    fastStartServer = true
  },
  {
    # 标识数据源的名字
    name = "note"
    drivelClassName = "com.mysql.cj.jdbc.Driver"
    url = "jdbc:mysql://49.232.150.194:3306/note"
    username = "root"
    password = "5201314zFy@"
    # initialSize = 5
    # maxActive = 50
    # minIdle = 5
    # 指示是否不用等待自己连接完毕立即启动
    fastStartServer = true
    # 指示启动使用快速启动可接受的最大延时
    startTimeOut = 50
  }
]
dataSource {
  drivelClassName = "com.mysql.cj.jdbc.Driver"
  url = "jdbc:mysql://49.232.150.194:3306/mybatis_plus"
  username = "root"
  password = "5201314zFy@"
  # initialSize = 5
  # maxActive = 50
  # minIdle = 5
  # 是否进行快速启动，设置为true将立即启动服务，再慢慢连接数据库，设置为false将会等待数据库连接完成再进行启动
  fastStartServer = true

}
